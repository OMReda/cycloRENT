# cycloRENT
A full-stack bike rental management web application built with Django (Python). Features fleet tracking, client management, booking lifecycle, real-time pricing via AJAX, and revenue analytics. Structured around the MVT pattern with Bootstrap 5 UI.
